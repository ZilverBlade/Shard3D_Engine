#include "../s3dpch.h" 
#include "level_gizmo.h"

namespace Shard3D {
	/*
	Gizmo::Gizmo(){}
	Gizmo::~Gizmo(){}
	void Gizmo::setContext(const sPtr<Level>& levelContext) {
		level = levelContext;
	}
	void Gizmo::render(LevelTreePanel treePanel) {
		Actor selectedActor = treePanel.getSelectedActor();
		if (selectedActor) {
		   // ImGuizmo::SetOrthographic(false);
		   // ImGuizmo::SetDrawlist();
		   // ImGuizmo::SetRect(ImGui::GetWindowPos().x, ImGui::GetWindowPos().y, ImGui::GetWindowWidth(), ImGui::GetWindowHeight());

			//auto editor_cameraActor = level->getEditorCameraActor();
		}
	}
	*/
}